$(window).ready(function() {
  // Copy this function from D
});

function randomRGBDot() {
  // Get the three random color numbers and return a dot with them in this format 'rgb(r,g,b)'
}

function randomColor() {
  // Return a random color (number) between 0 and 256
}
